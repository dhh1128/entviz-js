# SECURITY (F5+F6+F7+F8): affirmative 'identical' verdicts derived from prefix-blind channels — identityKey drops presentation prefixes, compareComparisonText affirms from cell text alone, and a Complete walk over a <=512-bit value contains no gestalt steps
kind: todo
tags: security
created: 2026-08-04T04:49Z

- 2026-08-04T04:49Z Full write-up: reviews/security-scan-2026-07-31/RESULTS.md F5 (compare.ts:46 identityKey), F6 (compare.ts:97 compareComparisonText), F7 (compare-walk.ts:296 buildCheckPlan, includeGestalt = d.truncated), F8 (EntvizCompare.ts:118 classifyResult). Scheduled to land WITH the spec-side folded-prefix work (entviz tick for the bech32 HRP): folding a prefix binds the gestalt but not the cells, so a cells-only walk or comparison text still cannot see it. F6 and F7 are the same defect from two directions.
- 2026-08-04T15:10Z Partly done 2026-08-04 in ebf1686. FIXED: F6 (comparisonText now leads with the bracketed top label; compareComparisonText compares the full text via comparisonText instead of re-deriving cells), F8 (flows through classifyResult unchanged), F7 (buildCheckPlan includes gestalt in EVERY Complete plan, unconditionally — the asymmetric case, a bare key vs did:key:<same body>, defeats any test conditioned on the value's own prefix). A label-less reference whose cells match ours returns unknown, not identical and not different. 405 core + 321 react tests pass, typecheck clean; comparison-design.md 6.1 documents the canonical format. STILL OPEN: F5. It is not a compare-layer bug — identityKey's comment is correct that a presentation prefix must not distinguish identity, or two inputs that render the same entviz would compare different. multihash 1220 vs 1b20 and the SSH algorithm header are swap-test questions about the PARSER, the same shape as the v16 HRP decision (entviz this.i:hrpb1nd) and probably a v17 spec item. The bech32 half of F5 resolves automatically when tick 4vmj lands the v16 port here.
