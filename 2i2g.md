# Port spec v17: derive the network qualifier from the input; let Cardano Shelley's 29-byte forms parse
kind: todo
tags: spec
created: 2026-08-05T00:16Z

- 2026-08-05T00:16Z Done 2026-08-05, local commit b77bdb0 (unpushed, no tag). Port of entviz e4ac328. (a) qualifiers.network is now DERIVED from the input on every blockchain path, never defaulted: segwit bc1/tb1; legacy base58 version byte 1,3 mainnet / m,n,2 testnet; Litecoin leading 't'; Cardano Shelley addr1|stake1 vs addr_test1|stake_test1. CashAddr was already correct. Cardano Byron deliberately carries NO network — its magic is inside the CBOR payload we do not decode. Key order (network before variant) preserved so the serialized data-qualifiers string is unchanged for mainnet. Visible effect: a testnet label gains the testnet mod ('BTC, testnet, tb1'). (b) CARDANO_SHELLEY_RE body floor 50 -> 45, so 29-byte reward/stake and enterprise addresses (47 chars) reach the Cardano parser instead of falling through to generic bech32 (mainnet) or the base64url fallback (testnet, because stake_test contains '_'). Tier A 104/104, Tier B 97/97 against the v0.17.0 corpus, full corpus, no skip list. Own suites: core 417/417 full gate, react 321/321, typecheck clean. Also bumped SPEC_VERSION, both workspace packages + playground + package-lock, both ci.yml ref pins to v0.17.0, and refreshed the vector counts in CERTIFICATION.md/README.md. NOT released to npm.
